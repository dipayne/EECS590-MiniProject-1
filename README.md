# EECS590-MiniProject-1
This mini-project models a finite Markov Reward Process using an abstract class and a tabular implementation. Value iteration is applied to compute state values via the Bellman update, illustrating how long-term expected rewards are estimated in a stochastic system
